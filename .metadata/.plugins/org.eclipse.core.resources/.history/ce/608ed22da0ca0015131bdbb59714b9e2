import java.util.Scanner;

public class maxContiSubsequence {
 static Scanner scan = new Scanner(System.in);
 static int arr[],m[];
 public static void main(String args[]){
	 int n=scan.nextInt(),maxSum=0,curSum=0;
	 arr= new int [n];
	 for(int i=0;i<n;i++)
		 arr[i]= scan.nextInt();
	 for(int i=0;i<n;i++){
		 curSum+=arr[i];
		 if(curSum<0)
			 curSum=0;
		 if(maxSum<curSum)
			 maxSum=curSum;
	 }
	 System.out.println(maxSum);
 }
 
 public static int withDP(int n){
	 m= new int[n];
	 if(arr[0]>0)
		 m[0]=arr[0];
	 else m[0]= 0;
	 for(int i=1;i<n;i++){
		 if(m[i-1]+arr[i]>0)
			 m[i]=m[i-1]+arr[i];
		 else m[i]=0;
	 }
	 for(int i=0;i<n;i++){
		 if
	 }
 }
}
